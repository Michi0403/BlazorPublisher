# PublisherStudio 3.4.9 — organic UI naming repair

- Changes the Organic Plugins return action from `Back to Publisher` to the consistent `Back to PublisherStudio` wording, using the already maintained localization key.
- Fixes the visible `PublisherStudio organ capabilities` typo to `PublisherStudio organic capabilities`.
- Adds the corrected exact-text localization key to every maintained catalog while retaining the historical typo key for compatibility.
- Preserves all existing routed `InteractiveServer` boundaries and avoids nested render-mode changes.
- No runtime, persistence, protocol, path-layout, FFmpeg, or deployment behavior is changed.
