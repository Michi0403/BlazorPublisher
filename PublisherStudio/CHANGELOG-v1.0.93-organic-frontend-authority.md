# PublisherStudio v1.0.93 — organic frontend authority and workspace proof

## Preserved PublisherStudio work

- Retains all v1.0.92 source files and records file-by-file workspace preservation evidence with zero missing baseline paths.
- Retains Panel/Div Studio live-content movement, dual video selection boundaries, clickable/editable sequence regions, responsive/flex liveboards, standalone HTML tooltip/hover/signal behavior, file-backed localization, and browser/page language controls.
- Retains the service-architecture registry null hardening, launch-profile port behavior, restored StreamingRuntimeUseCases source, FFmpeg capability fix, and complete shared protocol project.

## Organic link and permission workflow

- Updates PublisherStudio, installer, runtime metadata, tests, and package version to 1.0.93.
- Uses shared 1-Wire protocol 1.6 and the byte-identical LocalGPT wire project.
- Separates live TCP transport from a trusted organic link. PublisherStudio's frontend initiates the connection, then waits until the LocalGPT frontend user approves the link.
- Council, text generation, screen-reader help, and remote capability use remain disabled until the two-frontend link is complete.
- Adds clear `Awaiting LocalGPT approval` state and prevents non-Hello traffic while the transport is unlinked.
- Keeps PublisherStudio's receiving frontend authoritative for every advertised organ capability.
- The permission matrix controls per peer/capability/organ: exposure, invocation, required linked peer, frontend confirmation, editor, approval mode, and work-order scope.
- Plain-text, rich-text, and JSON information requested by LocalGPT is entered in the global approval bar and returned through the exact request/correlation ID before execution.
- Saved `SameCapability`, `CurrentWorkOrder`, and `AlwaysAllow` rules remain explicit user instructions; `Deny` is final and scheduler/sequential work cannot bypass the selected policy.

## Organic workflows retained

- PublisherStudio → LocalGPT Council request and reviewable `publisher.text.insert.propose` workflow.
- Screenshot/input eyes-and-hands capabilities with result polling for the next heartbeat.
- Single-flight recurring screen-reader assistance with a minimum 15-second interval and no request piling.
- Existing OpenSCAD graph/renderer, spreadsheet session inspection, FFmpeg/media capability, business context, and UI feature negotiation.
- Explicit connection-driven UI adaptation when LocalGPT is absent, waiting for approval, linked, disconnected, or advertising changed capabilities.

## Validation performed in this package

- Complete PublisherStudio `npm test` source-contract suite, including Panel/Media/export/localization and organic-plugin tests.
- Shared protocol byte equality, XML/JSON parsing, project-reference/source closure, conflict-marker scan, workspace preservation, and archive/source hash equality.

A native .NET/DevExpress build and browser runtime session were not available in the packaging environment and are not claimed.
