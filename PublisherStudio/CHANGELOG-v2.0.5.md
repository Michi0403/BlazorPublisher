# PublisherStudio 2.0.5 — Windows build and release-script correction

This release fixed the `SpreadsheetDocumentService` DI constructor, Windows PowerShell variable interpolation, and logging-policy classification for deterministic instance services.

Its compiler and build-policy fixes remain active. Any inherited custom installer or repair-manifest behavior is superseded by PublisherStudio 2.1.1.
