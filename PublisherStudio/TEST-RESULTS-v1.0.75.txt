PublisherStudio v1.0.75 validation results
==========================================

Validation date
---------------
2026-07-25

Scope
-----
This validation covers the InspectorPanel Razor RZ1010 repair, regression
protection for Razor control-flow ownership, release-version alignment,
unchanged dependency sets, patch reproducibility, and release archive integrity.

Automated Node contract suite
-----------------------------
Command:
  cd src/PublisherStudio.Web && npm test

Result: PASS

All 23 suites passed, including the C# compilation-safety suite. The new Razor
contract verifies that authoredLiveLayers, selectedLiveLayer and
selectedLiveFilter are normal C# declarations at the beginning of the existing
liveSource.IsVisual control-flow body. It also rejects the invalid nested
explicit `@{ ... }` block that caused RZ1010.

One existing Node warning remains: pictureStudioInterop.js is reparsed as an ES
module because package.json does not declare "type": "module". It does not fail
or alter the test result.

Static validation
-----------------
Result: PASS

- 38 non-vendor JavaScript and test modules passed `node --check`.
- 6 non-vendor JSON files parsed successfully.
- 2 project files parsed as XML successfully.
- No merge-conflict markers were found.
- No line-leading explicit `@{` Razor blocks remain in application components.
- Web application, installer, npm package, lock file, streaming runtime
  capability, and structured-export manifest versions align at 1.0.75.
- Publication format remains 1.52 and Picture Studio format remains 1.4.
- NuGet PackageReference, npm dependency, and npm devDependency sets are
  unchanged from v1.0.74.

Patch reproducibility
---------------------
Result: PASS

The final v1.0.75 Git-compatible source patch applies cleanly to the v1.0.74
release source with `git apply --check` and `git apply`. The patched result
matches the v1.0.75 work tree when excluding only Git metadata.

Release archive
---------------
Result: PASS

- ZIP structural integrity passed with `unzip -t`.
- A clean extraction matches the packaged v1.0.75 work tree.
- SHA-256 and SHA-512 checksum files are supplied beside the archive.

Compilation limitation
----------------------
A real .NET/Razor/DevExpress compile was not run in this environment because
`dotnet` and `csc` are unavailable and the licensed DevExpress restore feed is
not available. The reported RZ1010 construct was removed according to Razor
syntax rules and is covered by the repository regression contract. The release
should still be compiled on the normal licensed release machine before
production shipping.
