PublisherStudio v1.0.77 validation results

Scope
-----
C# compilation hotfix for the local-variable declaration-space collision in
MediaTimelineEditService.NormalizeTemporalSelection.

Automated regression suite
--------------------------
Command:
  cd src/PublisherStudio.Web && npm test

Result:
- All 24 npm/Node regression suites passed.
- Existing timeline, component, signal, pointer, map, font, zoom, streaming,
  interface, installer, persistence, usability, architecture, C# ownership,
  compilation-safety, interchange, media-gesture, Studio-overlay,
  component-drop, temporal-overlay, video-layer, project-import,
  structured-site, and VideoStudio interaction contracts remain green.

C# local-scope regression contract
----------------------------------
- NormalizeTemporalSelection uses candidateEnd for the provisional selection
  end inside the earlier nested block.
- The final normalized selection end remains the later end local.
- The compilation-safety test rejects reintroducing `var end` in the earlier
  nested block, guarding the reported CS0136 failure.

Static and format validation
----------------------------
- 36 non-vendor JavaScript/test/tool modules passed `node --check`.
- package.json and package-lock.json parse as JSON.
- All project files parse as XML.
- Application, installer, npm, lock-file, streaming runtime, and structured
  export manifest versions align at 1.0.77.
- Publication format remains 1.53.
- Picture Studio format remains 1.4.
- npm dependency and devDependency sets exactly match v1.0.76.
- `git diff --check` passed.

Patch validation
----------------
- The final Git-compatible v1.0.77 patch applies cleanly to an untouched
  v1.0.76 source tree.
- A replayed patch tree matches every tracked v1.0.77 source/release file
  byte-for-byte. Generated licensed vendor assets and per-user publish-profile
  files remain archive-only, as in the v1.0.76 baseline.

Archive validation
------------------
- BlazorPublisher_v1.0.77_csharp-scope-hotfix.zip passes ZIP CRC.
- A clean extraction matches the packaged v1.0.77 work tree byte-for-byte.
- SHA-256 and SHA-512 checksum files are supplied beside the archive.

Unavailable validation
----------------------
A real .NET/Razor/DevExpress compilation was not run in this environment
because the dotnet SDK and licensed DevExpress restore feed are unavailable.
The reported CS0136 statement was corrected directly and is covered by the
new source-level compilation-safety contract, but the release machine should
still run its normal `dotnet build` before deployment.
