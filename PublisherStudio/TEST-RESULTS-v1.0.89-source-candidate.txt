PublisherStudio v1.0.89 organic 1-Wire - source candidate validation
Date: 2026-07-26

PASS
- npm test from src/PublisherStudio.Web
  All existing PublisherStudio contract suites passed, including timeline, components,
  streaming, installer resilience, architecture, C# compilation-safety heuristics,
  OpenSCAD builder, browser automation/screenshot API, release gates, and the new
  organic plugin suite.
- New organic plugin suite verified LocalGPT UDP discovery resilience, TCP connection
  lifecycle generation guards, protocol integrity, capability negotiation, hard Deny and
  scoped permission rules, sequential/scheduled work coordination, explicit result status
  mapping, OpenSCAD Team flow, and spreadsheet hand-eye result packages.
- JSON/XML parsing, unresolved source-conflict scan, and targeted C# delimiter checks passed.

ENVIRONMENT LIMITATION
- This container does not contain the .NET SDK/MSBuild, Windows App SDK toolchain, or
  DevExpress build environment. Therefore this archive is a source candidate, not a
  compiler-verified binary release. The JavaScript and source-contract suite is green.
