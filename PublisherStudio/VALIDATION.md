# PublisherStudio 2.1.9 validation

The source validation covers installer asset selection, AppData layout, archive wrappers, deterministic release ZIP creation, version alignment, idempotent documentation source rewriting, repository-root GitHub Pages discovery, pinned snapshot version/PDF/link validation, build-guard consistency, architecture policies, and application regression contracts.

A successful owner-side Windows release build must still confirm the .NET 10 publishes, licensed DevExpress assets, DocFX HTML/PDF payload, all runtime archives, setup execution, exact GitHub release asset downloads, shortcuts, application startup, and the GitHub Pages Actions deployment.
