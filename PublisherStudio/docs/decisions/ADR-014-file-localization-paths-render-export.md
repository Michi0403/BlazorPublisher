# ADR-014: File localization, configurable paths and render export capabilities

**Status:** Accepted

PublisherStudio translations use simple JSON files beside the program plus DevExpress satellite assemblies. Media paths are resolved by a service with project overrides. Static render exports freeze live canvas/video state before rasterization, and export compatibility is described by an extensible capability service.
