// Backward-compatible entry point. The preparation now covers every local DevExpress
// browser asset plus the generated public DevExtreme runtime license.
await import("./prepare-devexpress-assets.mjs");
