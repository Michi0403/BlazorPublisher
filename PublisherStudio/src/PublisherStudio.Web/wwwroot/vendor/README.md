This directory is generated by `npm run prepare:spreadsheet`.

DevExpress and DevExtreme client assets are intentionally not committed or redistributed in the source archive. The build restores the matching 25.2.8 npm packages and copies them here so the published application runs fully offline.
