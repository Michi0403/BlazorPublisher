# PublisherStudio 2.6.4 StoryEditor compile repair

PublisherStudio 2.6.4 is a surgical build repair for the 2.6.3 feature release. It fixes the compiler-confirmed StoryEditor Razor string parse failure and the missing MainLayout NavigationManager injection without changing publication format or the implemented feature set.
