# PublisherStudio 3.4.3 source validation

Source-only validation verifies version policy, PowerShell compatibility guards, single startup trust validation, non-blocking notary recovery, StrictMode-safe Apple notary JSON/state access, common fresh/resume `notarytool info` polling, absence of direct optional status-property assumptions, documentation resume invariants, shared packaging ownership, and InteractiveServer architecture.

No PowerShell runtime, dotnet build, macOS signing, stapling, or Apple notarization execution is available in this validation environment.
